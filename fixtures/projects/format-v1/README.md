# format-v1 fixtures

Real fixture projects (`minimal`, `typical`, `large`, `edge-cases`,
`malformed`) are added in S2-T13. This file exists only so git tracks this
directory before then; see `fixtures/harness.fixtures.test.ts` and
`docs/testing-guide.md`.
