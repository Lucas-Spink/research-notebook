[package]
name = "nb-git"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[lints]
workspace = true

[dependencies]
gix = { version = "0.87.1", default-features = false, features = ["status", "sha1"] }
thiserror = "2"

[dev-dependencies]
proptest = "1.11.0"
