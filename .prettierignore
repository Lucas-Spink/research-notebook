# Machine-written; pnpm regenerates this in its own format.
pnpm-lock.yaml

# Hand-authored prose. Prettier's proseWrap would fight deliberately
# formatted technical writing (spec repo-tree blocks, ADRs), and several of
# these live under protected paths (AGENTS.md section 6) this task has no
# reason to touch.
**/*.md

# Machine-written by tauri-specta; regenerated in its own style on every
# dev run (AGENTS.md section 5, "Generated files" — never hand-edited).
apps/desktop/src/ipc/**

# Machine-written by scripts/export-format-schemas.mjs from the Zod schemas in
# packages/format; a test fails if they drift (AGENTS.md section 5, "Generated
# files" - never hand-edited).
docs/format/schemas/**

# Canonical output is compared byte for byte, so Prettier must not reflow it
# (flow maps, quoting). Golden tests own these files (AGENTS.md section 8).
packages/format/src/__golden__/**
